# ── Секреты и окружение ───────────────────────────────────────────────
.env
.env.local
.env.*.local
secrets/

# ── Python ────────────────────────────────────────────────────────────
*.pyc
*.pyo
__pycache__/
*.egg-info/
.venv/
venv/

# ── Сборки и зависимости ──────────────────────────────────────────────
dist/
build/
node_modules/
frontend/dist/
static/

# ── Данные и загрузки ─────────────────────────────────────────────────
postgres_data/
media/

# ── Логи и системное ──────────────────────────────────────────────────
*.log
.DS_Store

# ── Деплой-скрипты с доступами к серверу (пароль root, IP) ─────────────
# НЕ коммитим: содержат реальные учётные данные боевого сервера.
deploy_full.py
deploy_landing.py

# ── Внутренние генераторы деливери и дампы прод-данных ─────────────────
# pdf_build содержит выгрузку боевых промптов (интеллектуальная собственность),
# generate_* — генераторы клиентских документов, читают приватные данные.
pdf_build/
generate_*.py
*.docx
*.tar.gz

# ── Скриншоты админки и локальные снимки (могут содержать данные) ──────
admin-screens/
local-*.png
plaque-*.png
test_teeth.jpg

# ── Артефакты инструментов ────────────────────────────────────────────
.playwright-mcp/
